-- Services
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

-- Player
local player = Players.LocalPlayer

-- Function to print house manager with clear numbering
local function printHouseManagerNumbered()
    local clientData = require(ReplicatedStorage.ClientModules.Core.ClientData)
    local myData = clientData.get_data()[player.Name]
    local hm = myData.house_manager
    
    print("=== HOUSE MANAGER DATA ===")
    
    for key, value in pairs(hm) do
        print("MAIN KEY: " .. tostring(key))
        
        if type(value) == "table" then
            local index = 1
            for subKey, subValue in pairs(value) do
                print("   " .. index .. ". " .. tostring(subKey) .. " = " .. tostring(subValue))
                index = index + 1
            end
        else
            print("   VALUE: " .. tostring(value))
        end
    end
end

printHouseManagerNumbered()
