-- Services
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

-- Player
local player = Players.LocalPlayer

-- Recursive function to deeply inspect a table
local function deepInspect(tableData, indentLevel)
    indentLevel = indentLevel or 0
    local indent = string.rep("  ", indentLevel)
    
    for key, value in pairs(tableData) do
        local keyStr = indent .. "📁 " .. tostring(key) .. ": "
        
        if type(value) == "table" then
            local count = 0
            for _ in pairs(value) do count = count + 1 end
            print(keyStr .. "[TABLE with " .. count .. " items]")
            deepInspect(value, indentLevel + 1)
        else
            print(keyStr .. tostring(value))
        end
    end
end

-- Function to inspect HOUSE MANAGER more deeply
local function inspectHouseManagerDeep()
    print("🔍 DEEP INSPECTING HOUSE MANAGER")
    print("=========================================")
   
    local clientData = require(ReplicatedStorage.ClientModules.Core.ClientData)
    local data = clientData.get_data()[player.Name]
   
    -- Deep inspect house_manager
    if data.house_manager then
        print("🏠 HOUSE MANAGER:")
        deepInspect(data.house_manager, 0)
        print("")
    else
        print("🏠 No house_manager data found.")
    end
end

-- Run the function
inspectHouseManagerDeep()
