-- Simple timer script to run command every 2 minutes (120 seconds)

while true do
    -- Wait 2 minutes (120 seconds)
    wait(120)
    
    -- Execute your command
    game:GetService("ReplicatedStorage"):WaitForChild("API"):WaitForChild("AdoptAPI/ExitSeatStates"):FireServer(currentChar)
end
