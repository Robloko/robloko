-- Services
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

-- Player
local player = Players.LocalPlayer

-- Function to inspect all categories in player data
local function inspectAllPlayerData()
    print("🔍 INSPECTING ALL PLAYER DATA CATEGORIES")
    print("========================================")
    
    local clientData = require(ReplicatedStorage.ClientModules.Core.ClientData)
    local data = clientData.get_data()[player.Name]
    
    local categoryCount = 0
    for _ in pairs(data) do categoryCount = categoryCount + 1 end
    
    print("📊 Total categories: " .. categoryCount)
    print("")
    
    -- Inspect each category
    for categoryName, categoryData in pairs(data) do
        print("🎯 " .. categoryName)
        print("   Type: " .. type(categoryData))
        
        if type(categoryData) == "table" then
            local itemCount = 0
            for _ in pairs(categoryData) do itemCount = itemCount + 1 end
            print("   Items: " .. itemCount)
            
            -- Show sample of what's inside
            local sampleShown = 0
            for key, value in pairs(categoryData) do
                sampleShown = sampleShown + 1
                if sampleShown <= 3 then
                    if type(value) == "table" then
                        print("   📁 " .. key .. " [TABLE]")
                    else
                        print("   📄 " .. key .. " = " .. tostring(value))
                    end
                end
            end
            if itemCount > 3 then
                print("   ... and " .. (itemCount - 3) .. " more items")
            end
        else
            print("   Value: " .. tostring(categoryData))
        end
        print("")
    end
end

-- Run the function
inspectAllPlayerData()
