-- Services
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

-- Player
local player = Players.LocalPlayer

-- Function to recursively print table contents
local function printTable(t, indent)
    indent = indent or 0
    for key, value in pairs(t) do
        local formatting = string.rep("  ", indent) .. tostring(key) .. ": "
        if type(value) == "table" then
            print(formatting .. "[TABLE]")
            printTable(value, indent + 1)
        else
            print(formatting .. tostring(value))
        end
    end
end

-- Function to safely inspect chars_connected_to_me and states
local function safeInspectNestedTables()
    -- Use pcall to catch any errors
    local success, err = pcall(function()
        print("🔍 INSPECTING chars_connected_to_me AND states")
        print("===========================================")

        -- Safely require ClientData
        local clientDataSuccess, clientData = pcall(function()
            return require(ReplicatedStorage.ClientModules.Core.ClientData)
        end)
        if not clientDataSuccess then
            warn("Failed to require ClientData:", clientData)
            return
        end

        -- Safely get player data
        local dataSuccess, data = pcall(function()
            return clientData.get_data()[player.Name]
        end)
        if not dataSuccess then
            warn("Failed to get player data:", data)
            return
        end

        -- Access pet_state_managers
        local petStateManagers = data.pet_state_managers
        if not petStateManagers then
            print("❌ pet_state_managers not found in player data")
            return
        end

        -- Access the first pet entry
        local petEntry = petStateManagers[1]
        if not petEntry then
            print("❌ No pet entry found in pet_state_managers")
            return
        end

        -- Inspect chars_connected_to_me
        if petEntry.chars_connected_to_me then
            print("📋 Contents of chars_connected_to_me:")
            printTable(petEntry.chars_connected_to_me, 2)
        else
            print("❌ chars_connected_to_me not found in pet entry")
        end

        -- Inspect states
        if petEntry.states then
            print("\n📋 Contents of states:")
            printTable(petEntry.states, 2)
        else
            print("❌ states not found in pet entry")
        end
    end)

    if not success then
        warn("An error occurred during inspection:", err)
    end
end

-- Run the function
safeInspectNestedTables()
